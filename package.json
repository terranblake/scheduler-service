{
  "name": "@postilion/scheduler-service",
  "version": "1.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "flush": "rm -rf node_modules && rm package-lock.json",
    "clean": "npm run flush && npm install",
    "start": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@postilion/event-framework": "github:terranblake/event-framework",
    "@postilion/models": "github:terranblake/models",
    "@postilion/storage": "github:terranblake/storage",
    "@postilion/utils": "github:terranblake/utils",
    "bull": "^3.12.1",
    "moment": "^2.24.0",
    "nodemon": "^2.0.2"
  }
}
